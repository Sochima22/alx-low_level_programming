#include<stdio.h>
/**
 * main-Entry function*
 * Description:prints the alphabet in lowercase, followed by a new line.*
 * Return:Always 0 (sucess)
 */
int main(void)
{
char x = 'a';
while
	(x <= 'z');
{putchar(x);
	x++; }
	return (0); }
