#include<stdio.h>
/**
 * main-Entry function*
 * Description:prints the alphabet in lowercase, followed by a new line.*
 * Return:Always 0 (sucess)
 */
int main(void)
{
	int x; for (x = 'A'; x = 'Z'; x++);
{
	putchar(x); }
	return (0); }
