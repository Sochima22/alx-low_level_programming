#include<stdio.h>
/**
 * main-main entry*
 * Description:Both lower and upper case*
 * Return:Always 0 (success)
 */
int main(void)
{int y = 0;
char x;
while (y >= 0 && y <= 9)
{putchar(y);
	y++; }
	while (x >= 0x61 && y <= 0x62)
{putchar(x); }
putchar('\n');
return (0); }
