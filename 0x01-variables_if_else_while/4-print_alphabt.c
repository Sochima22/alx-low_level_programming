#include<stdio.h>
/**
 * main-Main function*
 * Description:Write a program that prints the alphabet in lowercase*
 * Return:Always 0 (success)
 */
int main(void)
{ int x;
	for (x = 'a'; x <= 'z'; x++)
	{if
		(x == 'e' && x == 'q')
		{break; }
		putchar(x);
		putchar('\n'); }
	return (0); }
